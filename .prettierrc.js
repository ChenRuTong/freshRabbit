module.exports = {
  // 单行长度
  printWidth: 120,
  // 缩进长度
  tabWidth: 2,
  // 句末使用分号
  semi: false,
  useTabs: false,
  // jsx中使用单引号
  singleQuote: true,
  // 如果对象中的至少一个属性需要加引号，就对所有属性加引号
  quoteProps: 'consistent',
  // 指定HTML文件的全局空格敏感度
  htmlWhitespaceSensitivity: 'strict',
  // 对vue中的script及style标签缩进
  vueIndentScriptAndStyle: true,
  // 将>多行 HTML（HTML、JSX、Vue、Angular）元素放在最后一行的末尾，而不是单独放在下一行
  bracketSameLine: true,
};
